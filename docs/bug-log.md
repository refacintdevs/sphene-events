# EventIQ — Bug Log

This file documents bugs encountered during development. It is maintained as a dissertation appendix artifact, demonstrating systematic identification, diagnosis, and resolution of real defects encountered across Phase 0 and Phase 1 of the project.

## Phase 0

| ID | Phase / Unit | Date | Bug Description | Root Cause | Fix Applied | Lesson Learned |
|----|-------------|------|-----------------|------------|-------------|----------------|
| BUG-001 | Phase 0, Unit 4 | 2026-05-25 | Initial seed script created only 2 vendor profiles instead of the spec'd 3 (the Decoration vendor "House of Lush" was missing), 4 services instead of 6, 0 portfolio items instead of the required 12, and the sample booking was in `PENDING_VENDOR` status instead of `PAID` with a corresponding `Payment` row. The database was technically populated but did not reflect the agreed seed state, which would have caused incorrect counts on the landing page and broken the booking flow demo. | The revised plan for Prisma 7 architecture changes — which introduced mid-flight clarifications about `prisma.config.ts`, adapter setup, and environment variable handling — did not carry forward the full seed data specification from the original plan. The AI assistant re-derived seed data from general context rather than from the explicit spec, producing a plausible but incomplete result. The root issue is context window fragmentation: prior decisions about seed data scrolled out of active context during a long multi-step plan. | The full seed specification was re-stated as a single self-contained block in a fresh instruction, listing every required record, field value, status, and date. The seed script was revised to use Prisma `upsert` (idempotent) rather than `create`, so re-running it is safe. After re-running, counts matched spec: 5 users, 3 vendor profiles, 6 services, 12 portfolio items, 1 booking (PAID), 1 payment (HELD). | Multi-step AI-assisted plans must consolidate all prior decisions into the active instruction, not assume that context established earlier in the session is still active. Long plans should be checkpointed with a single self-contained spec block before any data-population step. |
| BUG-002 | Phase 0, Unit 6 | 2026-05-26 | The `setUserRole` server action in `actions.ts` declared a TypeScript return type of `Promise<SetRoleError>`, implying the function always returns an error object. In practice, the function calls Next.js `redirect()` on success and therefore never returns normally on the happy path. The type contract was misleading: any caller reading the type signature would assume an error object is always returned and might incorrectly assume a missing return means failure. Although runtime behavior was correct, the type system was actively misleading. | Next.js `redirect()` works by throwing a special exception (`NEXT_REDIRECT`) that is caught by the framework rather than propagating to the caller. This means the function's return type can never actually be observed at runtime on the success path — the function exits via exception, not `return`. TypeScript cannot model "throws on success" in the return type without union-with-`never`, which was not applied. | Added a JSDoc comment above the function explaining that `redirect()` on success means the return type is only reached on error paths. The runtime behavior was not changed, as it was always correct; only the type contract documentation was clarified. A follow-up note was added to `code-standards.md` under the Server Actions section. | Server actions that call `redirect()` on success have an inherently non-obvious type signature. The pattern should be documented proactively in code standards so future actions are written with correct JSDoc from the start. Alternatively, use a discriminated union return type (`{ ok: true } \| SetRoleError`) and let the caller handle navigation explicitly — this is cleaner but more verbose. |
| BUG-003 | Phase 0, Unit 8 | 2026-05-28 | The sticky nav's mobile hamburger button rendered correctly and was visible at 375px width, but tapping it produced no visible response. The shadcn `Sheet` component that should have slid open to show the nav links never appeared. The button was interactive (focus styles applied), but the open/close toggle was non-functional, making the mobile nav completely inaccessible to users on small screens. | The `Sheet` was wired in controlled mode: the component received explicit `open={isOpen}` and `onOpenChange={setIsOpen}` props. With the installed combination of shadcn `Sheet` and the underlying Radix UI `Dialog` primitive at those versions, the `SheetTrigger` child's automatic toggle behavior was being suppressed when controlled mode was active and the initial state was `false`. The trigger fired `onOpenChange(true)` but the state update was not being batched correctly with the render cycle in the App Router streaming context. | Switched the `Sheet` to uncontrolled mode by removing the `open` and `onOpenChange` props entirely. Individual nav link items retained `SheetClose` wrappers so clicking a link still closes the sheet, which is the only programmatic control actually needed. The `isOpen` state variable was removed. Manual test at 375px confirmed the sheet opened and closed correctly after the fix. | Prefer uncontrolled mode for shadcn `Sheet` (and Radix Dialog primitives in general) unless there is a concrete requirement for programmatic control (e.g., closing the sheet from a parent component on route change). Controlled mode adds state management overhead and has version-specific edge cases with the App Router. |
| BUG-004 | Phase 0, Unit 8 | 2026-05-28 | The sticky nav had a `useEffect` scroll listener intended to switch the nav background from transparent to solid after scrolling past 10px. The nav rendered with a transparent background and never transitioned to solid, even after scrolling well past the threshold. This caused page content to visually bleed through the nav bar, making the header unreadable on pages with any content immediately below the fold. | Two compounding causes: (1) The `useEffect` registered the scroll event listener but did not call the handler immediately on mount, so `isScrolled` stayed `false` until the next scroll event. If the page loaded already scrolled (e.g., after a refresh or Clerk re-render mid-scroll), the initial state was always wrong. (2) When Clerk loaded and completed its auth state check, it triggered a React re-render of the `NavBar` component. The fresh component instance started with `isScrolled = false` and missed the scroll position that had already been established. Together, these meant the scroll state was lost on every auth-driven re-render. | The scroll handler was invoked once immediately inside the `useEffect` to set the initial state from the current scroll position. Additionally, `document.documentElement.scrollTop` was added as a fallback alongside `window.scrollY`, since Next.js App Router can route scroll state through the document element rather than the window in some configurations. Manual test confirmed correct behaviour after both a fresh load and a page refresh at a scrolled position. | Scroll listeners must call their handler once at mount time to capture the initial scroll position — registering the listener alone is not sufficient. Auth state changes in Clerk can trigger component re-mounts or re-renders that reset local state; components with scroll/position state must always initialize from the current DOM state rather than assuming `0`. |
| BUG-005 | Phase 0, Unit 4 (follow-up) + Vercel deployment | 2026-05-28 | The Vercel build pipeline failed during the `npm install` postinstall step (`prisma generate`) with the error: `PrismaConfigEnvError: Cannot resolve environment variable: DIRECT_URL`. This occurred even though `prisma generate` does not connect to a database — it only reads the schema to generate the TypeScript client. The build was not touching any database operation, yet it required a live database URL to be present. This blocked the first Vercel deployment entirely. | In Prisma 7, `prisma.config.ts` is a TypeScript module that is evaluated at import time when any Prisma CLI command runs, regardless of the subcommand. The file contained `env("DIRECT_URL")` to configure the migration datasource. Prisma 7 resolves all `env()` calls in `prisma.config.ts` eagerly at startup — before the CLI even determines whether the command needs a database connection. Every Prisma CLI invocation therefore requires all referenced environment variables to exist in the process environment, even for commands that never open a connection. Vercel's build environment had `DATABASE_URL` but not `DIRECT_URL`, which is intentional (DIRECT_URL is for migrations, not builds). | For local development: the postinstall script was updated to use `dotenv -e .env.local -- prisma generate` to load the variable from the local env file. For Vercel: `DIRECT_URL` was added to Vercel's build-environment variables (not the runtime environment) so it is available during `npm install`. The deployment succeeded on the first retry after this change. `env-variables.md` was updated to document that `DIRECT_URL` must be present in both local dev and Vercel build environments. | Prisma 7's configuration file uses eager environment variable evaluation. This is a breaking behavioral change from previous Prisma versions, where `datasource` blocks in `schema.prisma` were parsed lazily. Any project upgrading to Prisma 7 must audit all `env()` calls in `prisma.config.ts` and ensure those variables are available in every environment where any Prisma CLI command runs — including CI/CD pipelines and build systems. |
| BUG-006 | Phase 0, mid-development | 2026-05-25–26 | A file named `cls` (11 bytes, containing a pager dump of prior terminal output) appeared in the repository root and was accidentally committed in an early commit. The file had no purpose, cluttered the repo root, and added noise to the git history. It was not caught before commit because it resembled a short, lowercase file that could plausibly be part of a project. | While reviewing git log output inside Git's built-in pager (which uses the `less` pager and presents a `:` prompt at the bottom of the screen), the user typed `cls` intending to clear the PowerShell terminal. In the `less` pager, `:` followed by a filename writes the current pager output to that file — so `:cls` created a file named `cls` in the current working directory containing the pager's display buffer. The file was untracked and swept up in a subsequent `git add .` before the accidental artifact was noticed. | The `cls` file was deleted and the deletion was committed. To prevent recurrence, `git config --global core.pager "less -F -X"` was run: `-F` makes `less` exit immediately if the output fits on one screen (removing the pager prompt for short output), and `-X` disables terminal clearing on exit. With short git output no longer triggering the pager at all, the accidental `:filename` write pattern cannot recur. | Git's pager (`less`) has a different command vocabulary than the host shell. The `:` prompt is a `less` command interface, not a shell prompt — `q` exits, `:n` goes to the next file, and `:filename` writes to a file. Developers should configure Git's pager to auto-exit on short output (`less -F -X`) to avoid confusion, particularly on Windows where shell muscle memory for `cls` is strong. |
| BUG-007 | Phase 0, mid-development | 2026-05-25 | The `.env.example` file — which serves as a documented template for required environment variables and is intended to be committed to the repository as a developer onboarding artifact — was silently excluded from git tracking. `git status` showed it as untracked but `git add .env.example` had no effect; the file never appeared in `git diff --cached`. New developers cloning the repo had no template to understand what environment variables were needed. | The Next.js project scaffolder generates a `.gitignore` that includes the pattern `.env*` using a glob wildcard. This pattern matches `.env`, `.env.local`, `.env.development.local`, and so on — all of which should be ignored — but it also matches `.env.example`, which should not be ignored. Because the wildcard match takes precedence and there was no explicit allow-rule, `.env.example` was invisible to git. The issue was not immediately obvious because `git status` shows untracked-but-ignored files differently depending on `--ignored` flag usage. | The broad `.env*` wildcard in `.gitignore` was replaced with explicit per-file ignores: `.env`, `.env.local`, `.env.development`, `.env.production`, `.env.development.local`, `.env.production.local`, and `.env.test.local`. An explicit allow-rule `!.env.example` was added after the ignore block as a belt-and-suspenders measure. After the change, `git add .env.example` worked correctly and the file was committed. | Default `.gitignore` files generated by framework scaffolders should be treated as a starting point, not a finished configuration. Wildcard patterns like `.env*` are deliberately broad to protect against accidental secret commits, but they can suppress legitimate tracked files. Review and narrow any wildcard that could match files you intend to commit, and use `!` allow-rules for explicit exceptions. |
| BUG-008 | Phase 0, mid-development | 2026-05-25–26 | Git began producing CRLF-related warnings on virtually every file read or write operation on Windows: `warning: LF will be replaced by CRLF` or `warning: CRLF will be replaced by LF`. The working tree appeared constantly modified in `git status` even when no intentional changes had been made, producing false-positive diffs that obscured real changes. This made reviewing commits and confirming clean state unreliable. | PowerShell on Windows defaults to UTF-16 LE encoding with CRLF line endings for some file write operations. Git was configured with `core.autocrlf=true` (the Windows default), which converts LF → CRLF on checkout and CRLF → LF on commit. When files written by PowerShell (CRLF) were committed as LF and then checked out again as CRLF, the cycle was theoretically stable — but in practice, the mix of tools (Next.js dev server writing LF, PowerShell writing CRLF, Vercel expecting LF) created persistent churn. The Node.js toolchain and Vercel/Linux deployment target both expect LF exclusively. | Ran `git config core.autocrlf false` and `git config core.eol lf` to enforce LF line endings throughout the project, matching the Linux deployment target. A `.gitattributes` file was created with `* text=auto eol=lf` to normalize line endings for all text files at the repository level, independent of each developer's local git config. After running `git add --renormalize .`, all files were re-staged with consistent LF endings and the false-positive diffs disappeared. | Line endings should be standardized at the repository level via `.gitattributes` from the very first commit, not left to per-developer git config. On Windows, `core.autocrlf=true` is the git default but is wrong for projects that deploy to Linux. The correct project-level setting is `* text=auto eol=lf` in `.gitattributes`, which overrides local git config for any developer who clones the repo. |

## Phase 1

| ID | Phase / Unit | Date | Bug Description | Root Cause | Fix Applied | Lesson Learned |
|----|-------------|------|-----------------|------------|-------------|----------------|
| BUG-009 | Phase 1, Unit 1.4 | 2026-06-03 | Multiple auth failures with one root cause. (1) New signup via /onboarding/role hanged 4–8s then returned INTERNAL_ERROR. Terminal showed `PrismaClientKnownRequestError P2002 "Unique constraint failed on the fields: (email)"` thrown from `ensureUser`'s `db.user.upsert` in `src/lib/auth.ts`. (2) This happened when a Clerk user was deleted from the Clerk dashboard and the same email re-used for a new account; Clerk issued a new `clerkId` but the old DB row (old `clerkId`, same email) persisted. (3) Returning users who already had a role were bounced back to `/onboarding/role` because `onboarding/role/page.tsx` checks Clerk `publicMetadata.role` to skip the page — if the Clerk metadata write in `setUserRole` had previously failed (AD-004 logs failure but does not surface it), the DB had a role but Clerk did not, so the page rendered again. (4) An admin whose Clerk metadata was manually set and whose DB row was manually set to ADMIN still could not access `/admin` because the P2002 collision in sync caused every auth attempt to fail before the DB role could be read. | `ensureUser` uses `db.user.upsert({ where: { clerkId } })` with a single lookup key. When a Clerk user is deleted and re-created with the same email, Clerk assigns a new `clerkId`. The upsert finds no row by the new `clerkId`, attempts a CREATE, and violates the `email @unique` constraint on the existing row. There was no reconciliation path for this case. The `update: {}` clause (intentionally empty to preserve role on re-auth) is correct and is NOT part of the bug. The Clerk metadata write failure (Symptom 3) was incorrectly assessed as "cosmetic" — see BUG-010. | Added a catch block in `ensureUser` for `Prisma.PrismaClientKnownRequestError` with `code === "P2002"`. On collision: look up the existing row by `email`, update only its `clerkId` to the new Clerk-issued value, and return it. Role and all other fields are preserved — Clerk is the identity authority for `clerkId`; the DB is the source of truth for everything else (AD-004 contract unchanged). If the seed admin row has the same email as the developer's Clerk account, the reconciliation also automatically transfers the ADMIN role to the new account without any manual DB edit. The `update: {}` clause in the normal upsert path was kept unchanged. Build passes clean. | (a) Deleting a Clerk user does NOT delete the corresponding DB row. Any lazy-sync design that stores user data in a separate DB must handle the "deleted-and-re-created with same email" case, because Clerk will issue a new `clerkId` for a re-registration. (b) Sync logic must reconcile identity across both `clerkId` AND `email`: `clerkId` is the primary join key (fast, unique), but `email` is the fallback identity signal when the `clerkId` doesn't match any existing row. (c) Sync must never clobber role or user-set fields on re-auth: the `update` clause in an upsert-based sync must be explicitly empty or limited to fields that are safe to overwrite (e.g., `updatedAt`). Role is owned by the DB and must only change through explicit role-assignment actions. |
| BUG-010 | Phase 1, Unit 1.4 (follow-up) | 2026-06-03 | A returning user who had previously completed role onboarding signed out and back in, then landed on a blank `/onboarding/role` page that stayed blank indefinitely (~120s). No error appeared in the terminal. Server logs showed: `POST /sign-in 200`, `GET /onboarding/role 200 in 837ms`, `GET / 200`, `GET / 200`. The prior BUG-009 fix note incorrectly assessed this as "cosmetic" (re-selecting the same role is idempotent). It is not cosmetic: the page was functionally inaccessible. | Two compounding issues. (1) Blank page / 837ms delay: `page.tsx` called `currentUser()` (a live Clerk API call) at the top of the Server Component before streaming any content. With no Suspense boundary, the browser receives nothing for the full duration of that API call (~800ms). In Next.js App Router, RSC redirects return HTTP 200 with the redirect embedded in the payload — so the log shows `200`, not `307`. The `GET / 200` entries are the client following the RSC redirect to the homepage. The "indefinitely blank" state is the combined wait of the Clerk API call on `/onboarding/role` plus slow homepage DB queries (five queries, no Suspense) on `/`. (2) Invariant 8 violation: the redirect check read `clerkUser.publicMetadata?.role` (Clerk metadata) instead of the DB role. A user whose Clerk metadata write had previously failed (AD-004 known gap) would have a correct DB role but no Clerk metadata, causing `page.tsx` to render the role-selection UI instead of redirecting. For VENDOR/ADMIN users in the stale-Clerk state, this forced them to re-select their role on every login — or worse, prevented access to their dashboard entirely. | Rewrote the redirect logic in `page.tsx` in two tiers. Tier 1 (fast path): call `auth()` which reads the session JWT with no network round-trip. If `sessionClaims.metadata.role` is set, redirect immediately — no Clerk API call, no DB query, no blank-page delay. This covers returning users on the happy path. Tier 2 (slow path, stale-Clerk case): if JWT has no role, call `getCurrentUser()` which reads the DB role (invariant 8 compliant). VENDOR or ADMIN in DB → redirect based on DB, regardless of Clerk metadata state. CUSTOMER in DB with no JWT role → show role-selection page (new user or stale-Clerk CUSTOMER; re-selecting is idempotent). The `currentUser()` Clerk API call is still made inside `getCurrentUser()` for user-sync purposes, but it is no longer on the critical path for returning users. Note: the CUSTOMER-in-DB case still showed the role page — see BUG-011 for the follow-up. | (a) RSC redirects in Next.js App Router log as HTTP 200, not 307 — the redirect is encoded in the RSC payload. Never diagnose redirects from HTTP status codes in App Router server logs. (b) Anything awaited at the top of an async Server Component before JSX is returned blocks the entire response stream. Long-running calls (Clerk API, DB) must be wrapped in Suspense or moved to a lower position in the tree if any initial content can be shown first. (c) "Re-selecting the same role is idempotent" does not make the UX acceptable — it still forces the user through an extra flow step and can expose them to a blank-page race. A wrong assessment of "cosmetic" delays the fix. (d) JWT claims (`sessionClaims`) are the correct tool for stateless routing decisions on fresh sessions; `currentUser()` is needed only when the JWT might be stale (e.g., metadata changed mid-session). For onboarding redirect logic triggered immediately after sign-in, the JWT is always fresh. |
| BUG-011 | Phase 1, Unit 1.4 (follow-up to BUG-010) | 2026-06-03 | After BUG-010 was fixed, a returning CUSTOMER who signed out and back in was still sent to `/onboarding/role` and forced to re-pick CUSTOMER before reaching the homepage. Reproduced on a clean account against a freshly reset+seeded DB, ruling out stale data. Tier 1 (JWT fast path) missed the role, and Tier 2 (DB path) rendered the role-selection UI instead of redirecting — even though the CUSTOMER had a valid DB role from their original sign-up. | Two root causes. (1) Tier 1 miss: The JWT `sessionClaims.metadata.role` check correctly handles returning VENDOR/ADMIN but silently fails for CUSTOMER when the Clerk `publicMetadata` write inside `setUserRole` does not propagate to the next JWT (either the Clerk Admin SDK write fails silently per AD-004, or there is a propagation lag between `updateUserMetadata` and the session token issued on the next sign-in). (2) Tier 2 structural gap — the fundamental issue: the BUG-010 fix's Tier 2 called `getCurrentUser()`, which internally calls `ensureUser()`. `ensureUser()` *creates* the DB row for any Clerk-authenticated user who doesn't have one yet, defaulting to role CUSTOMER. This made the DB-role CUSTOMER state ambiguous: it is the same whether the user is genuinely new (never picked a role) or a returning customer with a stale-Clerk DB record. The BUG-010 fix only redirected VENDOR and ADMIN from Tier 2; CUSTOMER fell through to render the role page precisely because the code couldn't distinguish "new user" from "returning customer." The comment "Re-selecting is idempotent" was technically true but did not account for the Tier 2 call to `getCurrentUser()` erasing the "new user" signal. | Replaced `getCurrentUser()` in Tier 2 with `findCurrentUser()` — a new read-only function added to `src/lib/auth.ts` that calls `db.user.findUnique({ where: { clerkId: userId } })` WITHOUT calling `ensureUser()`. This restores a clean invariant: a DB row only exists after the user has explicitly called `setUserRole()` (the only code path that calls `ensureUser`). The Tier 2 logic became: if a DB row exists at all, redirect based on its role (ADMIN → /admin; CUSTOMER or VENDOR → /); if no DB row, the user is genuinely new — show the role-selection page. The role-selection page NO LONGER creates a DB row on load; the row is created only when the user submits `setUserRole()`. `findCurrentUser()` was exported from `src/lib/auth.ts` and imported in `page.tsx` replacing `getCurrentUser`. Build passes clean. | The distinction between "new user who has never picked a role" and "returning user with a DB role" depends entirely on whether a DB row exists. Any code that creates the DB row as a side effect of checking the role (as `getCurrentUser() → ensureUser()` does) destroys this signal. Role-selection pages and any other "first-time user" gates must use a read-only DB check, not an upsert-on-read. Design the creation side-effect as a deliberate step in the success path (here: inside `setUserRole`) rather than as a lazy default. |
| BUG-012 | Phase 1, Unit 1.4 (final resolution of BUG-010/011 saga) | 2026-06-03 | After BUG-010 and BUG-011 were fixed (in-render redirect logic was correct, DB row distinction was correct), a signed-in returning CUSTOMER still landed on a blank `/onboarding/role` page on first load after sign-in. URL bar stayed on `/onboarding/role`; F5 fixed it. Server logs showed `/onboarding/role` returning inconsistent `200` vs `307`. Confirmed NOT a logic bug — the DB row was found correctly. Confirmed to be a redirect-DELIVERY problem. | The root cause is how Next.js delivers `redirect()` from inside a Server Component versus how the browser receives it. When Clerk's post-sign-in flow does a client-side `router.push("/onboarding/role")` (RSC fetch, not a full page navigation), the server returns HTTP `200` with the redirect instruction embedded in the RSC payload. On the first post-auth load this RSC-embedded redirect sometimes fails to deliver cleanly to the browser router, leaving the browser URL stuck on `/onboarding/role` while the intended destination has already been fetched server-side. `NextResponse.redirect()` from the proxy middleware, by contrast, issues a real HTTP `3xx` response that the browser follows unconditionally, regardless of whether the original request was a full page load or a client-side RSC fetch. A secondary contributing factor: `setUserRole`'s write of `publicMetadata.role` to Clerk may not propagate to the JWT on the very next sign-in if the Clerk JWT session-token template does not include `"metadata": "{{user.public_metadata}}"` — meaning `sessionClaims.metadata.role` is always undefined even after a successful Admin SDK write, so the in-page Tier 1 fast-path never fires. | Moved the "user already has a role, skip /onboarding/role" redirect into `proxy.ts`. Added `isOnboardingRoleRoute = createRouteMatcher(["/onboarding/role"])` and a new gate block (AD-009): for authenticated requests where `sessionClaims.metadata.role` is set, issue `NextResponse.redirect()` to the correct destination before the page renders. Unauthenticated requests are redirected to sign-in. Requests with no JWT role are let through to page.tsx Tier 2 (`findCurrentUser` DB fallback, stale-JWT case). Page.tsx Tier 1 (JWT) and Tier 2 (DB) are kept as defense-in-depth but are no longer on the critical path for the common returning-user case. Existing admin gate and public-route matchers are unchanged. Build passes clean. | (a) In Next.js App Router, `redirect()` from a Server Component during a client-side RSC fetch does NOT produce an HTTP redirect — it embeds a redirect instruction in the RSC payload (HTTP 200). The browser router must follow this instruction, and on first post-auth load it sometimes does not. Never rely on in-page Server Component redirects as the sole mechanism for routing returning users away from authentication/onboarding pages. (b) The proxy middleware is the correct location for route-level access control and redirect decisions. It runs before any page code, on every request type (full page load AND RSC fetch), and always issues real HTTP redirects. (c) To diagnose whether Clerk JWT carries `publicMetadata`: check `sessionClaims` from `auth()` — if `sessionClaims.metadata` is always undefined even for users with known publicMetadata, the Clerk dashboard JWT session-token template is missing `"metadata": "{{user.public_metadata}}"`. |
| BUG-013 | Phase 1, Unit 1.4 (seed follow-up) | 2026-06-03 | Approving Bright Clicks Studio via the admin verification queue correctly updated its `verificationStatus` to APPROVED, but the vendor still did not appear on the public `/vendors` browse page after approval. No error was shown. The admin queue also showed no category for the vendor. | `prisma/seed.ts` created the Bright Clicks Studio `VendorProfile` row in Unit 1.4 but created zero `Service` rows for it. The `/vendors` search query in `src/services/vendor.ts` filters results to vendors with `services: { some: { isActive: true } }` — a vendor with no services is permanently invisible regardless of verification status. The admin queue derives displayed category from the vendor's services; no services → no category shown. The `VendorProfile` row existed and was visible in the admin queue, giving the false impression the vendor was fully seeded. Root cause: the Unit 1.4 seed addition was intentionally minimal (no portfolio items, no services) to test the empty-state UI, but that intentional minimalism crossed into breaking the approve-then-appear flow without any explicit note. Second instance of a seed linkage defect (see BUG-001: seed data that exists at the row level but is missing FK-linked child rows that other queries require). | Added two active `PHOTOGRAPHY` services for Bright Clicks Studio to `prisma/seed.ts` (`seed_service_007`: "Wedding Photography Package" — ₦200,000 / 20,000,000 kobo, 8h; `seed_service_008`: "Engagement & Portrait Session" — ₦70,000 / 7,000,000 kobo, 2h). Captured the `VendorProfile` create result in `v4Profile` (previously the return value was discarded, making it impossible to reference the profile's DB-generated `id` for FK linking). Updated the summary log line from `(2 catering, 2 photography, 2 decoration)` to `(2 catering, 4 photography, 2 decoration — Bright Clicks adds 2)`. Service IDs follow the stable `seed_service_NNN` pattern for idempotency across re-seeds. | (a) This is the second seed linkage defect. BUG-001 was missing portfolio items and a booking; BUG-013 is missing services. The recurring pattern: a seeded record is verified by checking that a row was created, not by verifying the FK-linked child records that downstream queries actually require. Seed validation should walk the dependency graph — verify that every seeded entity appears correctly in the queries that will actually be run against it (search, detail, admin queue). (b) "Intentional empty state for UI testing" is a legitimate seed decision, but it must be explicitly limited to UI concerns that do not affect query visibility. If a record must be visible to be useful in a flow (approve-then-appear), it must have the minimum required child data, even for a pending/draft entity. (c) Always capture the return value of `prisma.something.create()` if any subsequent seed step needs to reference the created record's generated `id`. Discarding the return value is a code smell in seed scripts. |
| BUG-014 | Phase 1, Unit 1.4 (true resolution of the BUG-010/011/012 auth saga) | 2026-06-04 | After the BUG-012 proxy.ts redirect was deployed, a returning user with a role STILL landed on a blank /onboarding/role on first sign-in. The proxy redirect did not fire. Every JWT-role-based check (in-page Tier 1 AND the new proxy gate) silently failed for all users. | The Clerk Dashboard session-token template (Sessions → Customize session token) was empty (`{}`). It did not include the line `"metadata": "{{user.public_metadata}}"`. Without it, Clerk never embeds publicMetadata in the session JWT, so `sessionClaims.metadata` is always undefined regardless of whether `setUserRole`'s Admin SDK write to publicMetadata succeeds. Every role-from-JWT check — page.tsx Tier 1 and the BUG-012 proxy gate — read undefined and fell through. The code fixes in BUG-009 through BUG-012 were necessary and correct, but none could work for the common case until the JWT carried the role. This was a Clerk Dashboard CONFIGURATION gap, not a code defect — which is why no code change resolved it. | Added `{ "metadata": "{{user.public_metadata}}" }` to the Clerk session-token template in the Clerk Dashboard. After a fresh sign-in (new token), verified: returning customer → straight to homepage, no blank page, URL is /; new user → role page renders and selection works; admin → reaches /admin; signed-out and signed-in non-admin → both still 404 on /admin (gate intact). Terminal confirmed the proxy now redirects before /onboarding/role renders at all. | (a) When a provider supplies data to your app via a token (Clerk JWT, OAuth claims, etc.), the data must be explicitly configured into that token — a successful write to the provider's store (publicMetadata) does NOT mean the value reaches your app. (b) Auth/routing bugs that survive multiple correct-looking code fixes are a strong signal to check provider DASHBOARD configuration, not just code. This saga cost ~2 days largely because the empty session-token template was not checked until late. (c) Verify the token actually carries what you expect early: log/inspect sessionClaims once, before building redirect logic that depends on it. |
| BUG-015 | Phase 1, Unit 2.1 | 2026-06-06 | Vendor onboarding submit silently did nothing — clicking "Submit application" on step 4 produced no network request (blank Network tab), no spinner, no error. The form appeared frozen. | Cloudinary uploads in step 3 are asynchronous, but step 3's "Continue" button had no guard against being clicked while an upload was still in flight. Clicking "Continue" before the government-ID upload resolved caused the parent OnboardingForm to snapshot step 3's state (via onNext) while the file reference was still null. Step 3's own UI later showed the file as uploaded (its local state updated after the upload resolved), but the parent had already captured the null reference. At submit, OnboardingForm.handleSubmit hit `if (!governmentId) return;` and returned silently — no POST, no feedback. The "uploaded" UI gave false confidence the reference had propagated. | Each upload widget (FileUpload, MultiFileUpload) now reports its in-flight state to the parent via an onUploadingChange callback. Step3VerificationDocs tracks govIdUploading/cacUploading/portfolioUploading; their OR (anyUploading) disables both "Continue" and "Back" and shows a "Waiting for uploads to finish…" hint while any upload is in flight. This makes it impossible to advance the step with an unresolved (null) file reference. As defense in depth, the previously-silent `if (!governmentId) return;` guard now shows a toast and routes the user back to step 3 instead of dead-ending. | In multi-step forms with asynchronous uploads, step transitions must be blocked until uploads resolve. A UI that shows a file as "uploaded" is not proof the file reference has propagated to the parent/submit state — local component state and lifted parent state can diverge across an async boundary. Guard the transition, not just the display. Also: a submit handler must never silently `return` — an early exit with no user feedback is indistinguishable from a frozen app. |
| BUG-016 | Phase 1, Unit 2.1 | 2026-06-06 | Even when bank details were filled in on step 4, the submitOnboarding Server Action received empty strings for bankName, bankAccountNumber, and bankAccountName. Server-side Zod validation correctly rejected them, but the failure surfaced only as a vanishing toast, making it look like "nothing happened." | Steps 1–3 lift their state to the parent OnboardingForm by calling onNext(data) with their validated values. Step 4's handoff signature was inconsistent: its onSubmit prop took no argument (`onSubmit: () => void`). Step 4 validated and held its values in its own internal state (v) but never passed them up; the parent's s4 state retained only its initial prefill values (empty for a new vendor) and was never updated. submitOnboarding was therefore called with the empty parent s4, even though the user had filled the fields and step 4's own validation passed against its populated internal state. | Changed step 4's handoff to match the onNext(data) pattern used by the other three steps: onSubmit now takes the validated Step4Values (`onSubmit: (data: Step4Values) => void`), and OnboardingForm.handleSubmit receives and uses that data instead of reading stale parent s4. The unused setS4 was removed. Separately, server-side VALIDATION_ERROR responses now route fieldErrors to the correct step and jump the user there, instead of showing only a generic toast. | In a lifted-state multi-step wizard, every step must use an identical handoff signature. One step with an inconsistent contract (here, passing no payload) silently drops that step's data with no type error and no runtime error — the data is simply absent. When several components implement the same role, enforce one shared interface so a deviation can't compile. |

> **Build-invisible defects:** BUG-015 and BUG-016 both produced zero `npm run build` errors or warnings — TypeScript was fully satisfied in both cases. They surfaced only during live browser testing. This reinforces the project rule (see Session Notes, 2026-05-28): a clean build does not verify feature correctness for UI units; manual browser testing is mandatory before marking any UI unit done.
